<section id="accueil" class="hero">
  <div class="hero__content" data-reveal-group>
    <div class="hero__kicker">
      <span class="hero__kicker-line"></span>
      Côte d'Ivoire — depuis 2020
    </div>
    <h1 class="hero__title">
      Agir pour la dignité,<br /><em>bâtir l'avenir</em>
    </h1>
    <p class="hero__lead">
      L'Initiative Nationale pour le Développement Communautaire - INADEC est une organisation non gouvernementale ivoirienne, engagée
      depuis 2020 au service de la justice sociale, de la dignité humaine et d'un développement économique
      inclusif en Côte d'Ivoire.
    </p>
    <div class="hero__actions">
      <a href="#projets" class="btn btn-primary">Découvrir le projet phare</a>
      <a href="#don" class="btn btn-outline">Devenir partenaire</a>
    </div>
    <div class="hero__axes">
      <div class="hero__axis">
        <div class="hero__axis-label">Axe 1</div>
        <div class="hero__axis-value">Santé et éducation des filles</div>
      </div>
      <div class="hero__axis hero__axis--bordered">
        <div class="hero__axis-label">Axe 2</div>
        <div class="hero__axis-value">Cohésion sociale et paix</div>
      </div>
      <div class="hero__axis hero__axis--bordered">
        <div class="hero__axis-label">Axe 3</div>
        <div class="hero__axis-value">Développement résilient</div>
      </div>
    </div>
  </div>

  <div class="hero__visual">
    <img
      src="/projets/ceremonie-ambassade-canada.png"
      alt="Cérémonie de signature à l'Ambassade du Canada en Côte d'Ivoire"
      class="hero__image"
      data-reveal="scale"
    />
    <div class="hero__quote" data-reveal>
      « Investir dans les filles, c'est investir dans l'avenir de nos communautés »
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    min-height: 640px;
    border-bottom: 1px solid var(--color-border);
  }

  .hero__content {
    padding: 88px 64px 80px 48px;
    max-width: 760px;
    margin-left: auto;
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 30px;
  }

  .hero__kicker {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-green-light);
  }

  .hero__kicker-line {
    width: 34px;
    height: 1px;
    background: var(--color-orange);
  }

  .hero__title {
    font-family: var(--font-serif);
    font-weight: 400;
    font-size: 66px;
    line-height: 1.03;
    letter-spacing: -0.02em;
    margin: 0;
    color: var(--color-green-darkest);
    text-wrap: pretty;
  }

  .hero__title em {
    font-style: italic;
    color: var(--color-green-light);
  }

  .hero__lead {
    font-size: 18.5px;
    line-height: 1.6;
    color: var(--color-text-body);
    margin: 0;
    max-width: 560px;
    text-wrap: pretty;
  }

  .hero__actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }

  .hero__axes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
    column-gap: 28px;
    row-gap: 22px;
    border-top: 1px solid var(--color-border);
    padding-top: 26px;
    margin-top: 8px;
  }

  .hero__axis--bordered {
    border-left: 1px solid var(--color-border);
    padding-left: 28px;
  }

  .hero__axis-label {
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin-bottom: 6px;
  }

  .hero__axis-value {
    font-size: 14.5px;
    font-weight: 600;
    color: var(--color-green);
  }

  .hero__visual {
    position: relative;
    border-left: 1px solid var(--color-border);
    min-height: 560px;
    display: grid;
    place-items: center;
  }

  .hero__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition-duration: 1.1s;
  }

  .hero__quote {
    transition-delay: 0.3s;
  }

  .hero__quote {
    position: absolute;
    bottom: 36px;
    left: -64px;
    background: var(--color-green);
    color: var(--color-cream);
    padding: 26px 30px;
    max-width: 360px;
    box-shadow: 0 18px 40px rgba(0, 64, 31, 0.2);
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 20px;
    line-height: 1.4;
    color: var(--color-bg);
  }

  @media (max-width: 1100px) {
    .hero__title {
      font-size: 52px;
    }
  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      min-height: auto;
    }

    .hero__content {
      margin-left: 0;
      max-width: none;
      padding: 56px 20px 48px;
    }

    .hero__visual {
      border-left: none;
      border-top: 1px solid var(--color-border);
      min-height: 420px;
    }

    .hero__quote {
      left: 20px;
      right: 20px;
      max-width: none;
      bottom: 20px;
    }
  }
</style>
